{
  "name": "b",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "cd backend && nodemon index.js",
    "web": "cd web && npm start",
    "requiremodules": "npm i && cd backend && npm i && cd ../web && npm i",
    "all": "npm-run-all --parallel backend web"
  },
  "author": "mehmet salih akcan",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
